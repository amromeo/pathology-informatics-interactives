# Topic 3 browser review — September 9–10, 2026

Reviewed the production Pages build in the Codex in-app browser at 1365 × 900
and 390 × 844. Screenshots are viewport captures, not full-page composites.

- Security: named accounts alone cover 2/5 scenarios (one blocked, one partial).
  The reference portfolio covers 5/5 with ransomware partial. Adding network
  segmentation exceeds the limits and disables Run. Reset clears the portfolio
  and restores early debrief access. Desktop and phone stress results captured.
- Downtime: all three controlled phases unlock six identity matches and three
  hazards. An empty reconciliation remains unready even with the preferred
  resumption choice. Correct assignments resolve 9/9. Changing identifier and
  communication choices resets selections, adds a duplicate chart, and removes
  the call log. The uncontrolled path has ten scored items.
- Browser review found that the missing-call branch incorrectly accepted linking
  nonexistent call documentation. The branch now requires verification and
  escalation. Retest: the old action leaves 9/10; the new action gives 10/10.
  A regression test covers the branch and confirms the controlled action remains
  unchanged. The debrief distinguishes selected plans from completed real work.
- Native action selects truncate long text. Selected action text now wraps
  immediately beneath the control; the phone capture shows this alongside the
  duplicate-chart hazard and 10/10 result.
- Privacy: a diagnosis-only filter cannot confirm isolation. Ewing sarcoma plus
  ZIP 02108 isolates NR-001. Key sequestration leaves the smallest group at one;
  all three transformations raise it to four. The public-release shortcut passes
  1/5; a controlled proposal missing purpose and recipient passes 4/5; completing
  all safeguards passes 5/5 and requests documented privacy review. Reset disables
  the release-check button again.
- At phone width, the document scroll width is 375 CSS pixels within the
  390-pixel viewport (the remaining space is the scrollbar). Wide evidence
  tables use their own scroll regions. No page-wide overflow was observed.
- No browser warning or error logs were returned for the reviewed tab.

Automated checks: 49 tests pass, curriculum validates 23 lessons and 34/34
objectives, production and Pages builds pass. Vite reports the existing large
JavaScript chunk advisory; this is not a build failure.

Scope: Chromium desktop and responsive-width review, not physical-device or
screen-reader certification. Lesson 2 captures outside this folder predate this
review and were not included in the Topic 3 change.

Publication verified: commit a40d33b, Pages run 34425084509 succeeded. The three
lesson routes render their bespoke controls in the public browser. Home, lesson
and faculty routes, and the referenced JavaScript/CSS assets returned HTTP 200.
